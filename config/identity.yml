# Bootstrapped values for the Slack -> ClickUp daily routine.

# Your Slack user ID. Used to detect when YOU say "yes".
my_slack_user_id: "U0AGHPHK8S1"   # @vilca (vilca@understoryagency.com)

# Slack user IDs of internal teammates whose "yes" also counts as a commitment.
# Special value "*" = treat every non-asker in the thread as an internal
# affirmer (matches "consider everyone else as yes" from setup).
internal_teammate_ids: "*"

# Slack channel ID for #claude-tasks (the private review channel).
review_channel_id: "C0B4PFDR4EQ"

# ClickUp workspace (team) ID.
clickup_workspace_id: "9011424132"

# ClickUp list ID for the "Claude Inbox" triage list. Fallback when a Slack
# channel isn't in config/channels.yml.
# URL: https://app.clickup.com/9011424132/v/l/li/901113803597
claude_inbox_list_id: "901113803597"
